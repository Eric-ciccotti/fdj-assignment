import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'fdj-assignment',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
